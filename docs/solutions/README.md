# Documented Solutions

These files preserve engineering lessons and reusable patterns. Some were
written during the Mini-Anchor predecessor and intentionally cite historical
package names, branches, and paths.

Apply the principles, but verify every concrete path and implementation claim
against the current `src/anchor`, `web/src`, tests, and
`../CURRENT_STATE.md`. These documents are not live project-status records.
